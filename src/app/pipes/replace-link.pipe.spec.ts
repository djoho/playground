import { ReplaceLinkPipe } from './replace-link.pipe';

describe('ReplaceLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
